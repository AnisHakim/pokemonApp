{
  "extends": "./tsconfig.app.json",
  "exclude": [],
  "compilerOptions": {
    "composite": true,
    "lib": [],
    "types": ["node", "jsdom"]
  },
  "include": [
    "src/**/*.ts",      // Inclure tous les fichiers TypeScript
    "src/**/*.d.ts",     // Inclure les fichiers de définition TypeScript
    "src/**/*.vue"       // Inclure les fichiers Vue.js single-file components
  ]
}
